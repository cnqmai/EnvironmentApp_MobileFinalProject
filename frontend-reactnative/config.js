// Cấu hình Base URL sử dụng localhost và cổng mặc định của Spring Boot (8080).
// Expo Tunnel sẽ tự động ánh xạ địa chỉ này đến máy Host của bạn.
const API_BASE_URL = 'http://localhost:8080/api';

export default API_BASE_URL;