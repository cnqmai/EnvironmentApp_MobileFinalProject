server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/environment_db?stringtype=unspecified
    username: env_app_user
    password: "2112"
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com"
            client-secret: "YOUR_GOOGLE_SECRET"
            scope:
              - email
              - profile
          facebook:
            client-id: "YOUR_FACEBOOK_APP_ID"
            client-secret: "YOUR_FACEBOOK_APP_SECRET"
            scope:
              - email
              - public_profile

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# --- CẤU HÌNH CÁC BIẾN MÔI TRƯỜNG ---

# Fix lỗi 1: Cho Service upload
file:
  upload:
    dir: uploads

# Fix lỗi 2: Cho WebConfig
app:
  upload:
    dir: uploads

facebook:
  api:
    user-info-uri: "https://graph.facebook.com/me?fields=id,name,email,picture.type(large)"

jwt:
  secret:
    key: MjAxNS0xMDExY2h1eWVuZGVtb2JpbGUxMGRpZW0xMjM0NQ== 
  expiration:
    ms: 86400000

aqi:
  api:
    key: 5ad9ae819b67abf939f3e0d4604bd362
    base-url: https://api.openweathermap.org/data/2.5/air_pollution
    geocoding-url: https://api.openweathermap.org/geo/1.0/reverse

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    com.enviro.app: DEBUG

# ... (các cấu hình khác)

# --- Gemini Configuration ---
gemini:
  api-key: "AIzaSyAtXwVcMg-h08LOcE17sK8gDINtHBMxHmY" 
  # Hãy thay thế "YOUR_GEMINI_API_KEY_HERE" bằng mã khóa thực tế của bạn