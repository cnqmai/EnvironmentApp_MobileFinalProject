

spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=update

# Cổng server
server.port=8080
server.servlet.context-path=/api

# Kết nối Database PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/environment_db
spring.datasource.username=env_app_user
spring.datasource.password=2112 

# --- CẤU HÌNH QUAN TRỌNG ĐỂ NẠP DỮ LIỆU ---
# 'create': Xóa sạch bảng cũ và tạo lại khi khởi động (Dùng để reset dữ liệu)
# Sau khi chạy thành công lần này, bạn nên đổi lại thành 'update'
spring.jpa.hibernate.ddl-auto=create

# Bắt buộc có dòng này để chạy data.sql sau khi Hibernate tạo bảng
spring.jpa.defer-datasource-initialization=true

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT & Logging
jwt.secret.key=MjAxNS0xMDExY2h1eWVuZGVtb2JpbGUxMGRpZW0xMjM0NQ==
jwt.expiration.ms=86400000
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Gemini AI (Nhớ điền key thật của bạn)
gemini.api-key=AIzaSyAipPCYeerflUWl-eOrQSnYDRc4c-unOh4
gemini.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent